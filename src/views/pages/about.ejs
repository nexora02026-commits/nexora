<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>NEXORA - About</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap"
      rel="stylesheet"
    />
    <link rel="icon" href="/images/agency logo.png" />

    <style>
      :root {
        --primary: #0a192f;
        --secondary: #00e5ff;
        --accent: #6c63ff;
        --bg: #f8fafc;
        --white: #fff;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: "Poppins", sans-serif;
        background: var(--bg);
        color: var(--primary);
        line-height: 1.6;
      }

      .hero {
        height: 45vh;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        background: linear-gradient(135deg, var(--primary), var(--accent));
        color: var(--white);
        padding: 0 1rem;
      }

      .hero h1 {
        font-size: 3rem;
        margin-bottom: 0.5rem;
      }

      .hero p {
        font-size: 1.2rem;
      }

      .container {
        max-width: 1000px;
        margin: 4rem auto;
        padding: 0 1rem;
      }

      .about-box {
        background: var(--white);
        padding: 2.5rem;
        border-radius: 15px;
        box-shadow: 0 15px 35px rgba(0, 0, 0, 0.08);
        text-align: center;
        margin-bottom: 3rem;
      }

      .about-box h2 {
        font-size: 2.4rem;
        margin-bottom: 1rem;
      }

      .about-box p {
        font-size: 1.1rem;
        color: #444;
      }

      .specialty {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 1.5rem;
        margin-bottom: 3rem;
      }

      .card {
        background: var(--white);
        padding: 2rem;
        border-radius: 15px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
        transition: 0.3s;
        text-align: center;
      }

      .card:hover {
        transform: translateY(-5px);
      }

      .card h3 {
        color: var(--accent);
        margin-bottom: 0.5rem;
      }

      .tech {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
        gap: 1.5rem;
      }

      .tech ul {
        list-style: none;
        margin-top: 1rem;
      }

      .tech li {
        padding: 0.4rem 0;
        color: #444;
      }

      footer {
        background: var(--primary);
        color: var(--white);
        padding: 3rem 2rem;
        text-align: center;
        margin-top: 4rem;
      }

      footer a {
        color: var(--accent);
        margin: 0 0.5rem;
        text-decoration: none;
      }

      @media (max-width: 768px) {
        .hero h1 {
          font-size: 2.2rem;
        }
      }
    </style>
  </head>

  <body>
    <header>
      <div class="logo">NEXORA</div>
      <nav>
        <ul class="nav-links">
          <li><a href="/">Home</a></li>
          <li><a href="/packages">Packages</a></li>
          <li><a href="/portfolio">Portfolio</a></li>
          <li><a href="/about" class="active">About</a></li>
          <li><a href="/contact">Contact</a></li>
        </ul>
        <div class="hamburger">
          <span></span>
          <span></span>
          <span></span>
        </div>
      </nav>
    </header>

    <style>
      :root {
        --primary: #0a192f;
        --accent: #6c63ff;
        --white: #fff;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      header {
        background: var(--primary);
        color: var(--white);
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem 3rem;
        position: sticky;
        top: 0;
        z-index: 1000;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2);
      }

      .logo {
        font-size: 2rem;
        font-weight: 700;
        letter-spacing: 1px;
        cursor: pointer;
      }

      nav {
        display: flex;
        align-items: center;
      }

      .nav-links {
        list-style: none;
        display: flex;
        gap: 2rem;
      }

      .nav-links li a {
        color: var(--white);
        text-decoration: none;
        font-weight: 500;
        padding: 0.3rem 0;
        position: relative;
        transition: color 0.3s;
      }

      .nav-links li a::after {
        content: "";
        position: absolute;
        left: 0;
        bottom: -3px;
        width: 0;
        height: 2px;
        background: var(--accent);
        transition: width 0.3s;
      }

      .nav-links li a:hover::after,
      .nav-links li a.active::after {
        width: 100%;
      }

      .nav-links li a:hover,
      .nav-links li a.active {
        color: var(--accent);
      }

      .hamburger {
        display: none;
        flex-direction: column;
        gap: 5px;
        cursor: pointer;
        margin-left: 1rem;
        z-index: 1100;
      }

      .hamburger span {
        width: 28px;
        height: 3px;
        background: var(--white);
        border-radius: 2px;
        transition: all 0.4s ease;
      }

      /* Mobile menu */
      @media (max-width: 768px) {
        .nav-links {
          position: fixed;
          top: 0;
          right: -100%;
          width: 100%;
          height: 100vh;
          background: rgba(10, 25, 47, 0.95);
          backdrop-filter: blur(5px);
          flex-direction: column;
          justify-content: center;
          align-items: center;
          gap: 2rem;
          transition: right 0.5s ease;
          z-index: 1000;
        }

        .nav-links.show {
          right: 0;
        }

        .nav-links li {
          opacity: 0;
          transform: translateX(50px);
          transition: all 0.4s ease;
        }

        .nav-links.show li {
          opacity: 1;
          transform: translateX(0);
        }

        .hamburger {
          display: flex;
        }

        .hamburger.active span:nth-child(1) {
          transform: rotate(45deg) translate(5px, 5px);
        }

        .hamburger.active span:nth-child(2) {
          opacity: 0;
        }

        .hamburger.active span:nth-child(3) {
          transform: rotate(-45deg) translate(5px, -5px);
        }
      }
    </style>

    <script>
      const hamburger = document.querySelector(".hamburger");
      const navLinks = document.querySelector(".nav-links");

      hamburger.addEventListener("click", () => {
        navLinks.classList.toggle("show");
        hamburger.classList.toggle("active");

        const links = navLinks.querySelectorAll("li");
        links.forEach((link, index) => {
          if (navLinks.classList.contains("show")) {
            link.style.transitionDelay = `${index * 0.1 + 0.2}s`;
          } else {
            link.style.transitionDelay = `0s`;
          }
        });
      });
    </script>

    <section class="hero">
      <h1>About NEXORA</h1>
      <p>We build. You grow.</p>
    </section>

    <main class="container">
      <div class="about-box">
        <h2>Who We Are</h2>
        <p>
          NEXORA is a modern web agency specialized in building high-performance
          websites and landing pages for small businesses. Our mission is to
          help brands grow online using clean design, smart user experience, and
          modern technologies.
        </p>
      </div>

      <div class="specialty">
        <div class="card">
          <h3>🚀 Our Specialty</h3>
          <p>Small Business Websites & High-Converting Landing Pages</p>
        </div>

        <div class="card">
          <h3>🎯 Our Mission</h3>
          <p>Deliver fast, modern, and effective digital solutions</p>
        </div>

        <div class="card">
          <h3>💡 Our Vision</h3>
          <p>Be the go-to agency for startups & small businesses</p>
        </div>
      </div>

      <div class="tech">
        <div class="card">
          <h3>Frontend</h3>
          <ul>
            <li>HTML5</li>
            <li>CSS3 / Tailwind</li>
            <li>JavaScript</li>
            <li>React (Small Projects)</li>
          </ul>
        </div>

        <div class="card">
          <h3>Backend</h3>
          <ul>
            <li>PHP + MySQL</li>
            <li>Firebase</li>
          </ul>
        </div>

        <div class="card">
          <h3>Tools</h3>
          <ul>
            <li>VS Code</li>
            <li>Git & GitHub</li>
            <li>Netlify / Railway</li>
          </ul>
        </div>
      </div>
    </main>

    <footer>
      <p>&copy; 2026 NEXORA. All Rights Reserved.</p>
      <div>
        <a href="https://www.instagram.com/nexora_1000/">Instagram</a>
        <a href="https://www.tiktok.com/@nexora_100">TikTok</a>
      </div>
    </footer>
  </body>
</html>
